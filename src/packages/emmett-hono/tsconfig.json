{
  "extends": "../../tsconfig.shared.json",
  "include": ["./src/**/*"],
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist", // Redirect output structure to the directory.
    "rootDir": "./src",
    "paths": {
      "@event-driven-io/emmett": ["../emmett/src"] // Adjusted path relative to this package
    }
  },
  "references": [
    {
      "path": "../emmett/"
    }
  ]
}
